function cipher(phrase){
  phrase=prompt('Ingrese una frase');
  //vamos a utilizar un for para que analize todo el string
  for(var i=0;i<phrase.length;i++){
    /*utilizamos un charCodeAt para obtener el indice de
    donde se encuentra las letras en el ASCII
     */
   position=phrase.charCodeAt(i);
   
 }
}

function decipher(displacement){

}
